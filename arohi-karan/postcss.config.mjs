/**
 * Present so Next does not walk up and pick up the PostCSS config belonging to
 * the unrelated project in the parent directory. This site uses no PostCSS
 * plugins of its own.
 */
export default { plugins: {} }
